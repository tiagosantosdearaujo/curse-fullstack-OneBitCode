//

const doubleVelocity = (velocity) => {
    return velocity * 2
}

alert(doubleVelocity(50))

//When there's only one line of code.

const tripleVelocity = (velocity) => velocity * 3

alert(tripleVelocity(50))

//When it has only one parameter, we don't need "(parameter)"

const quadrupleVelocity = velocity => velocity * 4

alert(quadrupleVelocity(50))